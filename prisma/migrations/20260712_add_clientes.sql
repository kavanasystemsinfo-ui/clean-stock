-- Registro de migración: Agregar columnas SaaS (clientes, id_cliente)
-- Este archivo está vacío porque la migración del schema SaaS se aplica vía supabase_init.sql

-- Los campos ya están en el schema principal (prisma/schema.prisma)